## Overview
- 完成 M2（AI 当日建议）：稳健解析 DeepSeek 返回，添加重试与安全 fallback，建议来源标记。
- UI：Step3 折叠切换移至标题行，建议卡片支持滚动以容纳长文本，移除卡内二次收起按钮。
- 解析改进：容错提取 category/description 字段，避免原始 JSON 渲染；日志记录 SHA-256 摘要与结构概况。
- 验证：建议生成空/格式错误时回落提示，生成中/无 Key/AI 关闭状态均有友好提示。

## Development process
1. 调整 Step3 折叠交互，标题区可展开/收起；去掉卡内收起按钮。
2. 为建议卡添加 `heightIn+verticalScroll`，长文本可滚动查看。
3. DeepSeek 解析：优先使用 `description/category`，扩展候选键，减少原始对象直出；保持空内容 fallback 逻辑。
4. 快速本地验证：跑 UI 预览与手动长文案，确认折叠与滚动正常。

## reference


## What's next
- 进入 M3：落地规则追问树，按症状插入动态问题；完善对应单元测试。
- 维护：保持 `./gradlew build` 通过后再迭代后续里程碑。
