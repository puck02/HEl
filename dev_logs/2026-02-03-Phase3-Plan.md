## 阶段3 实施计划 - 本地规则引擎 + AI分层系统

### 目标
将 AI 建议系统从"完全依赖 DeepSeek API"升级为"本地规则 + AI 深度分析"的分层架构，提高响应速度、降低 API 依赖、提升建议针对性。

### 核心原则
- **本地规则处理 70% 简单场景**：明确的指标阈值触发（睡眠<6h、运动次数=0、疼痛>7分）
- **AI 专注 30% 复杂模式**：周期性波动、多指标关联、趋势突变分析
- **反馈闭环**：历史追踪数据回流到 AI Prompt

---

## 子任务分解

### Task 3.1：创建 LocalAdvisorEngine

#### 职责
- 基于确定性规则生成基础建议
- 快速响应（无网络延迟）
- 覆盖常见健康指标阈值

#### 数据结构
```kotlin
// LocalAdvisorEngine.kt
data class LocalAdviceRule(
    val id: String,
    val category: String,              // sleep/exercise/diet/mood
    val triggerCondition: (DailyEntryWithResponses) -> Boolean,
    val adviceTemplate: AdvicePayload,
    val priority: Int
)

sealed interface LocalAdviceResult {
    data class Generated(
        val payload: AdvicePayload,
        val matchedRules: List<String>  // 触发的规则 ID 列表
    ) : LocalAdviceResult
    
    object NoMatch : LocalAdviceResult  // 无规则匹配，需要 AI 介入
}
```

#### 示例规则
```kotlin
val SLEEP_INSUFFICIENT = LocalAdviceRule(
    id = "sleep_lt_6h",
    category = "sleep",
    triggerCondition = { entry ->
        val sleepHours = entry.responses
            .find { it.questionId == "sleep_duration" }
            ?.answerValue?.toFloatOrNull() ?: 7f
        sleepHours < 6f
    },
    adviceTemplate = AdvicePayload(
        observations = listOf("睡眠时长不足6小时，可能影响恢复"),
        actions = listOf("今晚尝试提前30分钟上床", "睡前避免使用电子设备"),
        tomorrowFocus = listOf("记录实际入睡时间"),
        source = AdviceSource.LOCAL
    ),
    priority = 1
)
```

#### 实现文件
- **路径**：`app/src/main/java/com/heldairy/core/data/LocalAdvisorEngine.kt`
- **预计行数**：~200 行
- **依赖**：
  - `DailyEntryWithResponses`
  - `AdvicePayload`
  - `AdviceSource` 新增 `LOCAL` 枚举值

#### 公开 API
```kotlin
class LocalAdvisorEngine {
    fun generateAdvice(
        entry: DailyEntryWithResponses,
        weeklySummary: WeeklySummary?
    ): LocalAdviceResult
}
```

---

### Task 3.2：重构 DailyAdviceCoordinator 分层逻辑

#### 现状
```kotlin
// 当前流程（简化）
suspend fun generateAdvice(entryId: Long): Result<AdvicePayload> {
    val entry = repository.getEntryWithResponses(entryId) ?: ...
    val summary = summaryManager.generateFor7Days(entry.entry.entryDate)
    
    // 直接调用 DeepSeek API
    val response = deepSeekClient.chatCompletion(prompt, ...)
    return Result.success(response.choices[0].payload)
}
```

#### 目标流程
```kotlin
suspend fun generateAdvice(entryId: Long): Result<AdvicePayload> {
    val entry = repository.getEntryWithResponses(entryId) ?: ...
    val summary = summaryManager.generateFor7Days(entry.entry.entryDate)
    
    // 1️⃣ 先尝试本地规则
    val localResult = localEngine.generateAdvice(entry, summary)
    if (localResult is LocalAdviceResult.Generated) {
        return Result.success(localResult.payload)
    }
    
    // 2️⃣ 本地规则无匹配，调用 AI
    val enhancedSummary = enhancedAnalyzer.analyze(...) // 已有阶段1代码
    val effectivenessSummary = trackingRepository.generateEffectivenessSummary(...) // 阶段2代码
    
    val prompt = buildEnhancedPrompt(
        entry = entry,
        enhancedSummary = enhancedSummary,
        effectivenessSummary = effectivenessSummary  // 🔥 正式启用
    )
    
    val response = deepSeekClient.chatCompletion(prompt, ...)
    return Result.success(response.choices[0].payload)
}
```

#### 改动范围
- **文件**：`DailyAdviceCoordinator.kt`
- **行数变化**：+50 行（分层逻辑 + 反馈摘要启用）
- **新增字段**：`private val localEngine: LocalAdvisorEngine`

---

### Task 3.3：启用反馈摘要回流到 AI Prompt

#### 现状
`effectivenessSummary` 参数在 `buildEnhancedPrompt()` 中被声明但未使用（阶段2遗留）

#### 改动
```kotlin
// AdvicePromptBuilder.kt 修改
fun buildEnhancedPrompt(
    entry: DailyEntryWithResponses,
    enhancedSummary: EnhancedWeeklySummary?,
    effectivenessSummary: String? = null  // 阶段2预留参数
): String {
    val systemPrompt = """
    你是一位专业健康生活管家...
    
    ${if (effectivenessSummary != null) """
    
    【用户反馈历史】
    $effectivenessSummary
    
    请根据上述反馈调整建议策略：
    - 继续推荐用户标记为"有帮助"的建议方向
    - 避免重复用户标记为"无帮助"的建议类型
    - 参考执行效果评分优化建议强度
    """ else ""}
    """.trimIndent()
    
    // ... 构建 userPrompt
}
```

#### 改动文件
- `AdvicePromptBuilder.kt`
- `DailyAdviceCoordinator.kt`（调用 `generateEffectivenessSummary()`）

---

### Task 3.4：UI 层 - 每条建议独立反馈

#### 当前状态
- `AdviceResultCard` 仅显示 `AdvicePayload` 内容
- 无法获取每条建议的 `trackingId`

#### 改造方案

##### 步骤 A：扩展 AdviceUiState
```kotlin
data class AdviceUiState(
    val entryId: Long? = null,
    val entryDate: String? = null,
    val advice: AdvicePayload? = null,
    val trackingItems: List<AdviceTrackingEntity> = emptyList(),  // 🔥 新增
    val isGenerating: Boolean = false,
    // ... 其余字段
)
```

##### 步骤 B：ViewModel 加载 tracking 数据
```kotlin
// DailyReportViewModel.kt
private fun updateAdviceSnapshot(snapshot: DailyEntrySnapshot?, settings: AiSettings) {
    // ... 现有逻辑
    
    // 新增：加载该 entry 的 tracking 数据
    viewModelScope.launch {
        val trackings = snapshot?.entry?.id?.let {
            trackingRepository.dao.getTrackingsForEntry(it)
        } ?: emptyList()
        
        _adviceState.update { it.copy(trackingItems = trackings) }
    }
}
```

##### 步骤 C：UI 组件更新
```kotlin
// DailyReportScreen.kt 修改
@Composable
private fun AdviceResultCard(
    payload: AdvicePayload,
    trackingItems: List<AdviceTrackingEntity>,  // 🔥 新增参数
    onFeedback: (Long, UserFeedback) -> Unit,   // 🔥 回调
    generatedAt: Long?,
    isFallback: Boolean,
    isCollapsed: Boolean
) {
    // ...
    
    AdviceListBlockWithFeedback(
        title = "建议",
        items = payload.actions,
        trackings = trackingItems.filter { it.category == "actions" },
        onFeedback = onFeedback
    )
}

@Composable
private fun AdviceListBlockWithFeedback(
    title: String,
    items: List<String>,
    trackings: List<AdviceTrackingEntity>,
    onFeedback: (Long, UserFeedback) -> Unit
) {
    Column(verticalArrangement = Arrangement.spacedBy(4.dp)) {
        Text(title, style = MaterialTheme.typography.titleSmall)
        items.forEachIndexed { index, item ->
            val tracking = trackings.getOrNull(index)
            Row(
                modifier = Modifier.fillMaxWidth(),
                horizontalArrangement = Arrangement.SpaceBetween
            ) {
                Text("• $item", modifier = Modifier.weight(1f))
                
                if (tracking != null) {
                    Row(horizontalArrangement = Arrangement.spacedBy(4.dp)) {
                        IconButton(
                            onClick = { onFeedback(tracking.id, UserFeedback.HELPFUL) },
                            modifier = Modifier.size(32.dp)
                        ) {
                            Icon(
                                Icons.Default.ThumbUp,
                                contentDescription = "有帮助",
                                tint = if (tracking.userFeedback == UserFeedback.HELPFUL) 
                                    MaterialTheme.colorScheme.primary 
                                else 
                                    MaterialTheme.colorScheme.onSurfaceVariant
                            )
                        }
                        IconButton(
                            onClick = { onFeedback(tracking.id, UserFeedback.NOT_HELPFUL) },
                            modifier = Modifier.size(32.dp)
                        ) {
                            Icon(Icons.Default.ThumbDown, ...)
                        }
                        IconButton(
                            onClick = { /* 打开效果评分对话框 */ },
                            modifier = Modifier.size(32.dp)
                        ) {
                            Icon(Icons.Default.CheckCircle, ...)
                        }
                    }
                }
            }
        }
    }
}
```

##### 步骤 D：效果评分对话框
```kotlin
@Composable
fun EffectivenessRatingDialog(
    adviceText: String,
    onRating: (Int) -> Unit,
    onDismiss: () -> Unit
) {
    AlertDialog(
        onDismissRequest = onDismiss,
        title = { Text("执行效果评分") },
        text = {
            Column(verticalArrangement = Arrangement.spacedBy(12.dp)) {
                Text("建议：$adviceText")
                Text("执行后感觉如何？（1-5分）")
                
                // 星级评分组件
                Row(horizontalArrangement = Arrangement.Center) {
                    (1..5).forEach { score ->
                        IconButton(onClick = { onRating(score) }) {
                            Icon(
                                if (score <= selectedScore) Icons.Default.Star else Icons.Default.StarBorder,
                                contentDescription = "$score 分"
                            )
                        }
                    }
                }
            }
        },
        confirmButton = { /* ... */ },
        dismissButton = { /* ... */ }
    )
}
```

---

### Task 3.5：System Prompt 优化

#### 当前问题
AI 完全不知道用户的反馈历史，可能重复生成无效建议

#### 解决方案
在 `buildEnhancedPrompt()` 的 system prompt 中加入：

```kotlin
"""
【AI 能力边界】
你专注于识别以下复杂模式：
1. 周期性波动（如每周二疼痛加重）
2. 多指标关联（如运动量↑ → 睡眠质量↑）
3. 趋势突变（如连续7天指标稳定后突然恶化）

【不要重复简单建议】
本地规则已覆盖基础场景（睡眠不足、运动缺乏等），
请专注于上述复杂模式的洞察。

${if (effectivenessSummary != null) """
【用户反馈历史】
$effectivenessSummary

调整策略：
- ✅ 继续：用户标记"有帮助"且执行效果>4分的建议方向
- ❌ 避免：用户标记"无帮助"或执行效果<2分的建议类型
""" else ""}
"""
```

---

## 实施顺序

### 第一步：LocalAdvisorEngine 创建（独立开发）
- [ ] 创建 `LocalAdvisorEngine.kt` 文件
- [ ] 定义 `LocalAdviceRule` 数据类
- [ ] 实现 5-10 条常见规则（睡眠/运动/疼痛/情绪）
- [ ] 添加单元测试 `LocalAdvisorEngineTest.kt`
- [ ] 验证编译通过

### 第二步：DailyAdviceCoordinator 分层重构
- [ ] 在 `AppContainer` 中实例化 `LocalAdvisorEngine`
- [ ] 修改 `generateAdvice()` 流程（先本地，后 AI）
- [ ] 启用 `effectivenessSummary` 调用
- [ ] 更新 `buildEnhancedPrompt()` 使用反馈数据
- [ ] 编译验证

### 第三步：UI 层反馈界面
- [ ] 扩展 `AdviceUiState` 包含 `trackingItems`
- [ ] 修改 `updateAdviceSnapshot()` 加载 tracking 数据
- [ ] 创建 `AdviceListBlockWithFeedback` 组件
- [ ] 实现 `EffectivenessRatingDialog`
- [ ] 连接 ViewModel 回调
- [ ] 手动测试反馈流程

### 第四步：集成测试 + 优化
- [ ] 端到端测试：提交记录 → 生成建议 → 本地规则匹配 → 标记反馈 → 重新生成
- [ ] 验证 AI Prompt 中包含反馈摘要
- [ ] 性能测试：本地规则响应时间 < 50ms
- [ ] 优化规则优先级逻辑
- [ ] 更新文档

---

## 验收标准

### 功能验收
- [ ] 简单场景（睡眠<6h）触发本地规则，不调用 API
- [ ] 复杂场景（多指标异常）调用 AI，Prompt 包含反馈摘要
- [ ] 每条建议显示 👍/👎/✅ 按钮
- [ ] 点击 ✅ 弹出 1-5 星评分对话框
- [ ] 反馈成功后 Snackbar 提示
- [ ] 重新生成建议时，AI 根据历史反馈调整策略

### 性能验收
- [ ] 本地规则响应时间 < 50ms
- [ ] API 调用频率降低 ~60%（预期 70% 场景用本地规则）
- [ ] UI 反馈操作无感知延迟

### 代码质量
- [ ] LocalAdvisorEngine 单元测试覆盖率 > 80%
- [ ] 规则配置可扩展（便于后续添加新规则）
- [ ] System prompt 结构清晰，分层逻辑明确
- [ ] 无编译警告，`./gradlew build` 通过

---

## 风险评估

### 技术风险
1. **规则冲突**：多条规则同时触发时优先级不明确
   - 缓解：定义 `priority` 字段，按优先级排序
2. **AI 提示过长**：反馈摘要可能占用大量 token
   - 缓解：限制摘要为"最近30天Top5反馈"
3. **UI 复杂度**：每条建议独立反馈增加交互复杂度
   - 缓解：MVP 版本仅显示 3 个按钮，暂不支持编辑反馈

### 用户体验风险
1. **本地规则过于死板**：用户觉得建议千篇一律
   - 缓解：规则数量控制在 8-10 条，多数场景仍走 AI
2. **反馈按钮占用空间**：建议列表过长时不易阅读
   - 缓解：仅在 actions 列表显示反馈按钮，observations 不加按钮

---

## 预计工作量
- **Task 3.1**：4-6 小时（LocalAdvisorEngine + 测试）
- **Task 3.2**：2-3 小时（分层重构）
- **Task 3.3**：1-2 小时（反馈摘要启用）
- **Task 3.4**：6-8 小时（UI 改造 + 对话框）
- **Task 3.5**：1-2 小时（Prompt 优化）
- **集成测试**：2-3 小时
- **总计**：16-24 小时

---

## 后续优化（M7+）
- 规则配置 JSON 化（支持无代码更新规则）
- 反馈数据导出到 CSV（供医生查看）
- AI 自主生成新规则（基于高频反馈场景）
- 多语言支持（规则模板国际化）
